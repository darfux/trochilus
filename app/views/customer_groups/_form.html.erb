
<%= form_for(@customer_group) do |f| %>
  <% if @customer_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_group.errors.count, "error") %> prohibited this customer_group from being saved:</h2>

      <ul>
      <% @customer_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :customer do |c_f| %>
    <%= render 'customers/form', :f => c_f %>
  <% end %>

  <div>  
    <button id='go-select-customer'>选择客户</button>
  </div>

  <div id='group-customer-result'></div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<div class='modal-mask'>
</div>
  <div id='customer-select-panel'>
    <span class='close'>X</span>
    <div id='customer-select-panel-content'>
      <%= render 'customers/search' %>
    </div>
  </div>

  <div id='controller-mark' name="<%=params[:controller]%>"></div>
