<%= common_form do %>
<%= form_for(@donation_record_interest_period) do |f| %>
  <% if @donation_record_interest_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donation_record_interest_period.errors.count, "error") %> prohibited this donation_record_interest_period from being saved:</h2>

      <ul>
      <% @donation_record_interest_period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>%
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.date_select :start, start_year: default_start_year, default: Date.today.change(day: 1)  %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.date_select :end, start_year: default_start_year, end_year: Date.today.year+15, default: Date.today.change(day: 1)  %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% end %>