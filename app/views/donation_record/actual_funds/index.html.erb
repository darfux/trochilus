<h1>Listing donation_record_actual_funds</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @donation_record_actual_funds.each do |donation_record_actual_fund| %>
      <tr>
        <td><%= link_to 'Show', donation_record_actual_fund %></td>
        <td><%= link_to 'Edit', edit_donation_record_actual_fund_path(donation_record_actual_fund) %></td>
        <td><%= link_to 'Destroy', donation_record_actual_fund, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Actual fund', new_donation_record_actual_fund_path %>
