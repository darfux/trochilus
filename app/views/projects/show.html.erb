<%= content_for :nav_bar_function_area do %>
  <% if @current_user.user.user_type.downcase == :employee.to_s %>
    <%= link_to '编辑', edit_project_path(@project) %> |
    <%= link_to '返回', employee_manage_project_path %>
  <% end %>
<% end %>

<%= content_for :nav_bar_title do %>
    项目信息
<% end %>

<!-- <div id="project-info-header"></div> -->
<div id="project-info">
  <div class="info-block">
    <p>
      <div><strong>名称：</strong> <span><%= @project.name %></span></div>
      <div><strong>编号：</strong> <span><%= @project.serialnum %></span></div>
      <div><strong>基金类型：</strong> <span><%= @project.project_type.name %></span></div>
    </p>
    <p>
      <div><strong>基金级别：</strong> <span><%= @project.project_level.name %></span></div>
      <div><strong>是否留本：</strong> <span><%= @project.endowment.ch %></span></div>
    </p>
    <p>
      <div><strong>创建日期：</strong> <span><%= @project.create_date %></span></div>
    </p>
  </div>
  <hr>
  <div class="info-block">
    <p>
      <div><strong>创建单位：</strong> <span>数学研究所</span></div>
    </p>
    <p>
      <div><strong>电话：</strong> <span>0991-2280233</span></div>
      <div><strong>邮箱：</strong> <span>cheungmm@gmail.com</span></div>
      <div><strong>传真：</strong> <span>0991-2280233-1</span></div>
      <div><strong>邮编：</strong> <span>300071</span></div>
    </p>
  </div>
  <hr>
  <div class="info-block">
    <p>
      <div><strong>联系人：</strong> 
        <% @project.link_men.each do |lm| %>
          <span><%= link_to lm.name, project_link_man_path(@project.id, lm.id) %></span>
        <% end %>
        <%= link_to '添加', new_project_link_man_path(@project.id) %>
      </div>
    </p>
  </div>
  <hr>
  <div class="info-block">
    <p>
      <div><strong>概要：</strong> <span><%= @project.brief %></span></div>
    </p>
  </div>
</div>
<div><b>捐赠记录</b> <%= link_to '添加', new_donation_record_path(project: @project) %></div>
  <table class="employee-data-table">
    <tbody>
      <tr>
        <th>序号 </th>
        <th>编号</th>
        <th>捐赠人</th>
        <th>币种</th>
        <th>日期</th>
        <th>计划到账</th>
        <th>实际到账</th>
        <th>其他</th>
      </tr>

    <% @project.donation_records.each do |r| %>
              <tr>
        <td></td>
        <td></td>
        <td><%= r.customer.name %></td>
        <td><%= r.plan_fund.currency.name %></td>
        <td><%= r.plan_fund.time.localtime.to_s %></td>
        <td><%= r.plan_fund.amount %></td>
        <td><%= r.actual_amount %></td>
        <td><%= link_to "详情", r %>|<%= link_to '删除', r, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>

    <% end %>

    </tbody>
  </table>
  <div colspan="11">计划到账： <%= get_all_plan @project %> 已到账总额：<%= get_all_actual @project %></div>

  <p></p>

<div><b>使用记录</b> <%= link_to '添加', new_usage_record_path(project: @project) %></div>
  <table class="employee-data-table">
    <tbody>
      <tr>
        <th>序号 </th>
        <th>日期</th>
        <th>执行单位</th>
        <th>执行人</th>        
        <th>受益单位</th>
        <th>负责人</th>
        <th>使用金额</th>
        <th>用途</th>
        <th>文件</th>
        <th>其他</th>
      </tr>
      <% @project.usage_records.each do |r| %>
      <tr>
        <td>xx</td>
        <td><%= r.fund.time %></td>
        <td><%= r.exec_unit.name %></td>
        <td><%= link_to r.exec_manager.name, r.exec_manager%></td>        
        <td><%= r.benefit_unit.name %></td>
        <td><%= link_to r.benefit_manager.name, r.benefit_manager%></td>
        <td><%= r.fund.amount%></td>
        <td><%= r.usage_type.name%></td>
        <td>
          <input name="Confirm3" type="submit" value="查看">
        </td>
        <td><%= link_to '删除', r, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <div>总支出：210，000  项目现余额：190，000</div>



<% if @current_user.nil? %>
  <%= link_to '编辑', edit_project_path(@project) %> |
  <%= link_to '返回', projects_path %>
<% end %>
