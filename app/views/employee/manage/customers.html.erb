<%= content_for :nav_bar_title do %>
    客户管理
<% end %>
<%= content_for :nav_bar_function_area do %>
  <%= link_to '新客户', new_customer_path %>
<% end %>
<table class="employee-data-table">
  <tbody>
    <tr>
      <th>序号</th>
      <!-- <th>编号</th> -->
      <th>姓名</th>
      <th>
        类型
        <span class='filter'><%= select_tag :customer_type, 
          options_for_select({ "个人客户"=>:IndividualCustomer, "公司客户"=>:CorporateCustomer, "团体客户"=>:CustomerGroup}, 
          ((f=params[:filters]) ? f[:customer_type] : nil)), prompt: "全部" %></span>
      </th>
      <!-- <th>国籍</th> -->
      <th>联系电话</th>
      <!-- <th>总捐赠额</th> -->
      <!-- <th>评级</th> -->
      <!-- <td>其他</td> -->
      <% if current_user.account == 'fkadmin' %>
        <th>创建人</th>
      <% end %>
    </tr>
    <% counter=0 %>
    <% @customers.each do |c| %>
    <tr>
      <td><%= (counter+=1).to_s.rjust(2, '0') %></td>
      <!-- <td>X1234</td> -->
      <td><%= link_to c.name, c.customer %></td>
      <td><%= c.customer.class.model_name.human %> <%= judge_sf(c) ? '（校友）' : '' %></td>
      <!-- <td>中国大陆</td> -->
      <td><%= c.tel %></td>
      <!-- <td><%# c.total_donation %></td> -->
      <!-- <td>5*</td> -->
      <% if current_user.account == 'fkadmin' %>
        <td><%= c.creator.name %></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>