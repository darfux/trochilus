<%= content_for :nav_bar_title do %>
  客户信息编辑
<% end %>

<%= content_for :nav_bar_function_area do %>
  <% if current_user.user_type.downcase == :employee.to_s %>
    <%= link_to '返回', individual_customer_path(@self_customer.id) %>
  <% end %>
<% end %>

<%= render 'form' %>
