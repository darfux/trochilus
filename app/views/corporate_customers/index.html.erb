<h1>Listing corporate_customers</h1>

<table>
  <thead>
    <tr>
      <th>Customer</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @self_customers.each do |corporate_customer| %>
      <tr>
        <td><%= corporate_customer.customer %></td>
        <td><%= link_to 'Show', corporate_customer %></td>
        <td><%= link_to 'Edit', edit_corporate_customer_path(corporate_customer) %></td>
        <td><%= link_to 'Destroy', corporate_customer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Corporate customer', new_corporate_customer_path %>
