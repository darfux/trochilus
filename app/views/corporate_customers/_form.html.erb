<%= form_for(@corporate_customer) do |f| %>
  <% if @corporate_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corporate_customer.errors.count, "error") %> prohibited this corporate_customer from being saved:</h2>

      <ul>
      <% @corporate_customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.text_field :customer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
