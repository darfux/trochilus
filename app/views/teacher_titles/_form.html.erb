<%= form_for(@teacher_title) do |f| %>
  <% if @teacher_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_title.errors.count, "error") %> prohibited this teacher_title from being saved:</h2>

      <ul>
      <% @teacher_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
