<%= form_for(@online_customer) do |f| %>
  <% if @online_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@online_customer.errors.count, "error") %> prohibited this online_customer from being saved:</h2>

      <ul>
      <% @online_customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
