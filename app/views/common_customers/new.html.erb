<%= render "#{@SelfType.to_s.pluralize}/form" %>

<%= content_for :nav_bar_function_area do %>
  <% if session[:user_type] == :employee %>
    <%= link_to '返回', get_back_path(get_self_path) %>
  <% end %>
<% end %>
