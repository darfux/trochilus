<%= form_for(@fund_type) do |f| %>
  <% if @fund_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fund_type.errors.count, "error") %> prohibited this fund_type from being saved:</h2>

      <ul>
      <% @fund_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
